#include<stdio.h>
#include<math.h>
int main(){
    int i;
    scanf("%d",&i);
    if(i>1 && i%2!=0 && i%(sqrt(n))!=0 ){
        printf("Prime");
    }
    else{
        printf("Not Prime");
    }
}